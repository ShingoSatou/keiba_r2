# Open Hypotheses

- `eval score` を追加するなら、single-race API ではなく batch inspection で十分か
- `pl_feature_profile` の粒度を `stack_default` 以上にどこまで増やすか
- feature profile の命名を、単一差分中心に保つか、複合差分も許すか
- imported legacy seed から baseline run をどの粒度で再生成して固定するか
